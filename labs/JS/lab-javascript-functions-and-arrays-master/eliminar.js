


// const alphabet = ["f", "cutfutfiu", "vuh", "z"];
// let usingForEach = alphabet.forEach((eachLetterParameter) =>
//   console.log(eachLetterParameter + )

// );

// let total=0,
// numbers.forEach(function(i){total += i;});
// console.log(total);



// debe devolver la palabra cuando se la llama, 
// con un array de una sola palabra.

// debe devolver la primera aparición de la palabra 
// cuando la más larga tiene varias apariciones

const arr = [1, 2, 3, 4, 5, 1];


const usingFilterMethod = arr.filter((arrayElement) => {
    return arrayElement === 2 || arrayElement === 4;
  });
  console.table(usingFilterMethod);